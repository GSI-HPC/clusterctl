# Network topology constants — sourced by source_me.sh; export to bin/ scripts
# Used by sshuttle templates and bin/cluster-ipmi.
# Override any variable via environment before sourcing.

export CLUSTER_NET_IPMI="${CLUSTER_NET_IPMI:-10.0.0.0/8}"

# Subnets routed by the sshuttle configurations in var/sshuttle/
export CLUSTER_NET_CLUSTER1="${CLUSTER_NET_CLUSTER1:-}"
export CLUSTER_NET_SITE_EXT="${CLUSTER_NET_SITE_EXT:-}"
export CLUSTER_NET_INTERNAL="${CLUSTER_NET_INTERNAL:-}"
export CLUSTER_NET_SYS02_1="${CLUSTER_NET_SYS02_1:-}"
export CLUSTER_NET_SYS02_2="${CLUSTER_NET_SYS02_2:-}"
export CLUSTER_NET_SYS02_3="${CLUSTER_NET_SYS02_3:-}"
export CLUSTER_NET_SYS02_4="${CLUSTER_NET_SYS02_4:-}"
