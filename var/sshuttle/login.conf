${CLUSTER_NET_SITE_EXT:-}
${CLUSTER_NET_INTERNAL:-}
--remote
${CLUSTER_HOST_SSH_PROXY:-}
--dns
--disable-ipv6
--daemon
--pidfile
/tmp/sshuttle-login.pid
--exclude
${IP_EXCL_LOGIN_1:-}
--exclude
${IP_EXCL_LOGIN_2:-}
